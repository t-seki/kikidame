# Jellyfin Radio

Jellyfin サーバに音楽ライブラリとして取り込まれたラジオ録音を、番組単位のルールで自動ダウンロード／削除し、サーバに到達できなくても聴けるようにする Android アプリ。

## Language

**番組 (Program)**:
ラジオの一つの番組。複数の**各回**を持つ。Jellyfin 側では MusicAlbum として表現される。保持ルールは番組単位で決める。
_Avoid_: アルバム、シリーズ、フォルダ

**各回 (Episode)**:
番組の一回分の録音。ちょうど一つの**番組**に属する。Jellyfin 側では Audio として表現される。
_Avoid_: トラック、曲、アイテム、ファイル

**放送局 (Station)**:
番組を放送しているラジオ局。一つの**番組**はちょうど一つの放送局に属する。Jellyfin 側では MusicAlbum の AlbumArtist として、手元のフォルダ構成では番組フォルダの親フォルダとして表現される。
_Avoid_: アーティスト、アルバムアーティスト、出演者（各回ごとに変わる別の情報）

**放送日 (Aired At)**:
各回が本来放送された日時。各回の「新しさ」（並び順、「最新 N 回」の判定）はこれで決める。サーバがタグから放送日を得られないときは**取り込み日時**で代用する。サーバを経由していない各回は、ファイルのタグ → ファイル名に含まれる日付 → ファイルの更新日時の順で代用する。日単位の値であり、同じ番組の複数の各回が同じ放送日を持つことがある。その場合の順序は各回のタイトルの辞書順で決める。
_Avoid_: 作成日、追加日、DateCreated（取り込み日時であり放送日ではない）

**取り込み日時 (Added At)**:
サーバがその各回をライブラリに取り込んだ日時。サーバがタグから**放送日**を得られないときの代用にだけ使い、新しさの判定には使わない。
_Avoid_: 放送日と混同すること

**サーバ ID (Server Item ID)**:
Jellyfin が番組・各回に付ける識別子。アプリ内の同一性はこれに依存せず、サーバ ID を持たない番組・各回も正規の状態として存在できる。サーバ ID を持たない各回は「サーバに在る」と主張していないので、「サーバの一覧から消えた」という理由で削除されることはない。
_Avoid_: itemId（アプリ内の主キーと混同する）

**未結合 (Unlinked)**:
サーバ上の実体に結び付いていない手元の番組・各回。**サーバ ID** を持たない（サーバを経由せず手元に置かれた）か、持っているサーバ ID がサーバの完全な一覧に無い。**突合**の対象。
_Avoid_: 孤児、未紐付け、ローカル行

**突合 (Matching)**:
**未結合**の手元の番組・各回に、サーバ上の番組・各回のサーバ ID を結び付ける操作。番組は（放送局, 番組名）、各回は同じ番組内のタイトルの完全一致で結び付ける。タイトルで結べなかった各回は、同じ番組内で**放送日**が同じで尺がほぼ同じ（数秒以内）候補に結び付ける（尺が不明な回は対象外）。どちらの段でも候補が複数あれば結び付けない。手元の行と再生位置はそのまま残る。**同期**が削除を判断する前に行うので、結び付けられた各回は「サーバの一覧から消えた」とは扱われない。
_Avoid_: マージ、重複排除、名寄せ（表記ゆれの吸収はしない）

**同期 (Sync)**:
サーバ上の各回一覧と**保持ルール**から、手元に置くべき各回を決め、不足分をダウンロードし余剰分を削除する操作。サーバが各回の存在の正であり、手元はキャッシュである。
_Avoid_: バックアップ、ミラー

**同期対象 (Sync Enabled)**:
番組ごとに利用者が選ぶ「この番組を同期するか」の状態。既定では同期対象ではなく、同期対象の番組にだけ**保持ルール**が適用される。
_Avoid_: 購読、フォロー、自動ダウンロード ON

**保持ルール (Retention Rule)**:
**同期対象**の番組ごとに決める「どの各回を手元に置くか」の規則。「最新 N 回まで保持」と「再生済みなら削除」の二つがあり、独立した削除理由として働く（どちらかに該当すれば手元に置かない）。**固定**された各回には適用されない。適用されるのは**同期**のときだけで、再生済みになった瞬間や番組の設定を変えた瞬間には何も消えない。
_Avoid_: 同期設定、ダウンロード設定

**最新 N 回まで保持 (Keep Latest)**:
手元に置く各回の数の上限。再生済み・未再生を問わず、**放送日**の新しい順に N 回を超えた各回は手元に置かない。**固定**された各回は N に数えない（固定が 3 本あっても、最新 N 回は別枠で手元に置かれる）。
_Avoid_: 未読を N 件残す（未再生に限定する意味ではない）

**再生済みなら削除 (Delete After Played)**:
**再生済み**になった各回を手元に置かない規則。

**固定 (Pinned)**:
**保持ルール**による削除の対象外である各回の状態。利用者が手動でダウンロードした各回、およびサーバを経由せず手元に置かれた各回がこの状態になる。番組が同期対象かどうかに関わらず手元に置かれ、利用者が固定を外すか手動で削除するまで残る。ただし**サーバ ID**を持つ各回がサーバの一覧から消え、**突合**でも結び直せなかったときは固定でも各回ごと消える（固定は保持ルールを免れるだけで、サーバが存在の正であることは変えない）。
_Avoid_: お気に入り、保護、手動ダウンロード済み

**判断保留 (On Hold)**:
ある番組について同期がダウンロードも削除も行わない状態。番組の各回一覧を取得できなかったとき（**到達不能**）と、番組そのものがサーバの一覧から見つからないとき（**消失**）にこの状態になる。「一覧が空」とは区別する。
_Avoid_: オフライン（アプリ全体の状態と混同する）、スキップ

**到達不能 (Unavailable)**:
ある番組の各回一覧の取得に失敗した状態。**判断保留**の理由の一つ。
_Avoid_: エラー、タイムアウト

**消失 (Gone)**:
手元の番組が持つ**サーバ ID** がサーバの番組一覧に無く、**突合**でも結び付けられなかった状態（**未結合**の番組のうち、かつてサーバに在ったもの）。**判断保留**の理由の一つ。サーバ側の再取り込みや整理で起きるのが通常で、各回の削除とは区別する。
_Avoid_: 削除済み、孤児

**再生済み (Played)**:
各回を聴き終えたとみなしてよい状態。再生が進んで再生位置が末尾付近に達すると自動でこの状態になり、その後シークして戻しても自動では解除されない。手動で再生済み／未再生を切り替えられる。
_Avoid_: 視聴済み、完了、既読

**再生位置 (Playback Position)**:
各回をどこまで聴いたか。手元の記録が正であり、サーバ側の値には依存しない。**再生済み**の手動切替では変わらない。
_Avoid_: レジューム位置、進捗

## Example dialogue

> **Dev**: 「最新 3 回まで保持」の番組で、3 回のうち 1 回を再生済みにしたら？
> **Expert**: 再生済みなら削除が有効なら、その回は手元から消える。手元は 2 回になるが、最新 3 回まで保持は「上限」なので、これで正しい。

> **Dev**: では 4 回目の新しい回が来たら、3 回目は未再生でも消える？
> **Expert**: そう。上限が 3 なので、新しい順に 3 回を超えた分は未再生でも手元に置かない。聴き逃したくなければ N を増やす。

> **Dev**: 同期対象にしていない番組の昔の回を 1 つだけ聴きたい。
> **Expert**: 手動でダウンロードすればいい。それは固定された各回になるので、保持ルールでは消えない。同期対象の番組でも、固定した回は「最新 N 回」の外に出ても残る。

> **Dev**: サーバが落ちていて一覧が取れないときは？
> **Expert**: その番組は到達不能で判断保留。何も消さないし何も落とさない。「一覧が空」とは別物。

> **Dev**: サーバでライブラリを作り直して、番組の ID が全部変わったら？
> **Expert**: 手元の番組は未結合になり、放送局と番組名が一致する新しい番組に突合で結び直される。各回もタイトルで結び直されるので、ファイルも再生位置もそのまま。結び直せなかった番組だけが消失で判断保留になり、手元の回は消えない。「番組が見つからない」と「番組はあるがこの回が無い」は別の事実で、削除理由になるのは後者だけ。

> **Dev**: 番組はそのままで、各回の ID だけがサーバ側で変わったら？
> **Expert**: 各回が未結合になり、同じ番組内のタイトルで結び直される。結び直せなかった各回は本当にサーバから消えた回なので削除される。

> **Dev**: エンディングの曲を飛ばして止めたら再生済みになる？
> **Expert**: 末尾付近まで達していれば再生済み。冒頭に戻して聴き直しても自動では未再生に戻らない。戻したいなら手動で切り替える。

> **Dev**: 同じ番組の同じ日に 2 本の各回があるが、どちらが先？
> **Expert**: 放送日は日単位なので同着になる。順序はタイトルの辞書順で決める。録音側が「2026-06-12」「2026-06-12 (1)」と付けるので、それで前後が決まる。

> **Dev**: 「最新 3 回まで保持」の番組で古い回を 2 本固定している。手元は何本になる？
> **Expert**: 5 本。固定は保持ルールの外なので N に数えない。固定を 3 本以上しても最新 3 回は別枠で来る。

> **Dev**: 固定した回がサーバ側で削除されたら、手元には残る？
> **Expert**: 残らない。固定は保持ルールを免れるだけで、サーバが各回の存在の正であることは変わらない。二度と落とし直せないので各回ごと消える。サーバ ID を持たないシード由来の回は別で、サーバに在ると主張していないので消えない。

> **Dev**: 同期対象の番組で、保持されるはずの最新回のファイルを手で消したら？
> **Expert**: 次の同期で落とし直される。ファイル削除は容量を一時的に空ける操作で、「この回はいらない」の意思表示は再生済みにすること。
