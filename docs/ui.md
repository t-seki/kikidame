# UI の方針

#48（UI 全体の見直し）で決めた、アプリ全体の UI の方針。画面構成・動線・色・タイポグラフィの「何を大事にするか」と、画面ごとの役割を書く。用語は `CONTEXT.md` に従う（「お気に入り」「購読」「フォロー」など _Avoid_ に入っている語は UI のラベルに使わない）。

決定日: 2026-09-19。実装は sub-issue（#54〜#59）で行う。

## 大事にすること

1. **片手で「続き」か「未再生」をすぐ流せること。** 主な聴取シーンは寝る前・移動中で、腰を据えて番組を選ぶ時間は短い。操作は画面の下半分（親指が届く範囲）に寄せる
2. **学習コストを増やさないこと。** 番組 → 各回 → 再生 の 3 階層と、各画面の役割は変えない。動線の変更は「入口を減らす」方向だけ
3. **個性は構造ではなく色で出すこと。** dynamic color をやめ、固定パレットで「ラジオらしさ」を持たせる。構造を変えずに済むので実装コストが色・タイポの定義に閉じる
4. **手元／サーバの別、固定、消失は隠さないこと。** オフライン優先とフォルダ同期が差別化点なので、その状態は一覧の行に出したまま

## 色

固定パレット。ダークが主（寝る前の利用）で、ライトも同じ構成。既定は端末の設定に追従し、設定の「テーマ」で システム／ダーク／ライト を選べる（#62。「端末はライトのままアプリだけ常にダーク」のため）。

温かい黒（赤みのある黒）にアンバーのアクセント。状態を表す色は **`primary`（アンバー）と `error`（赤）の 2 系統だけ** にし、それ以外の意味を色に持たせない（聴いている回の行の背景だけ `secondaryContainer` で示す）。

| スロット | ダーク | ライト | 使いどころ |
| --- | --- | --- | --- |
| `primary` | `#F5A623` | `#9A5B00` | よく聴く ★・同期対象・聴いている回の印・節の見出し・進捗 |
| `onPrimary` | `#3D2600` | `#FFFFFF` | 再生ボタンのアイコン |
| `primaryContainer` | `#5A3A00` | `#FFDDB3` | 選択中のチップ |
| `onPrimaryContainer` | `#FFE2B8` | `#2E1B00` | |
| `secondaryContainer` | `#3A2D15` | `#F3E3C8` | 聴いている回の行の背景、ボトムシート内の選択 |
| `onSecondaryContainer` | `#F2DDB8` | `#2E2312` | |
| `surface` | `#141210` | `#FBF7F1` | 画面の地 |
| `onSurface` | `#EDE5DA` | `#1F1A14` | 本文 |
| `background` / `onBackground` | surface / onSurface と同じ | 同左 | Scaffold の地。既定のままだと Material3 の素の色が透ける |
| `onSurfaceVariant` | `#B3A58F` | `#5E5347` | 補足行・ラベル |
| `surfaceContainerLow` | `#1B1815` | `#F5F0E8` | |
| `surfaceContainer` | `#211D18` | `#EFE9DF` | TopAppBar の地。ミニプレイヤーはこれに `primary` を 18% 混ぜた面（#59） |
| `surfaceContainerHigh` | `#2B261F` | `#E8E1D5` | ボトムシート |
| `surfaceContainerHighest` | `#35302A` | `#E1D9CC` | 入力欄 |
| `error` | `#FF7A68` | `#B3261E` | 消失（雲）・ダウンロード失敗・破壊的操作 |
| `onError` | `#3B0906` | `#FFFFFF` | |
| `outline` | `#7A6E5C` | `#8A7D6C` | チップの枠 |
| `outlineVariant` | `#4A4136` | `#D6CCBC` | 区切り線 |

上に無いスロットは Material3 の `darkColorScheme()` / `lightColorScheme()` の既定のまま。実装は #54。

## タイポグラフィ

- 書体はシステムのまま（Material3 の既定 `Typography`）
- **数字は等幅（`tnum`）。** `未再生 3`・`1:23:45`・`12.3 GB` のように数が並ぶ場面が多いので、桁が揺れないようにする。全スタイルに付ける（#54）
- 再生画面の時間表示を大きくする案（後述の D 案）は採らない。色が入った後に実機で気になれば別 issue

## 画面ごとの役割

| 画面 | 役割 | #48 で変えること |
| --- | --- | --- |
| 番組一覧 | 聴く番組を選ぶ根の画面。「よく聴く／その他」の節、行に `局 · 未再生 N / 手元 L · 全 E 回 · 最新 MM-DD`（#41、変えない） | 絞り込みの入口を 1 つに統合（#55）。検索（#44）と局（#45）は 1 枚のシートに、選択中は検索語と局をそれぞれチップで |
| 各回一覧 | 番組の回を選ぶ・ダウンロード／固定／再生済みを操作する。右端のアイコンは状態表示＋最も自然な 1 操作。補足行は `放送日 · 出演者 · 尺 · 状態`（タイトルが放送日と同じ回は放送日を省く #66。出演者は #70 で足した。長い名前は折り返す） | 固定・再生中の小アイコンはタイトル前のまま、印の無い行にも同じ幅の枠を確保して開始位置を揃える（#56。補足行へ移す案は実機で「情報が増えて読みにくい」と却下）。タイトルは 1 行 |
| 再生画面 | 聴いている回の操作。左右スワイプでシーク（#29）。タイトルの下に `放送局 · 出演者`（#70。無い回も 1 行分の場所は確保する） | 倍速・再生済み・スリープをシークバー下の中央 3 列に（#57）。TopAppBar から再生済みを降ろす |
| ミニプレイヤー | 一覧の下で「聴いている回」を示し、再生画面へ戻る入口 | 上端に再生位置の線、面をアンバー系に（#59） |
| 設定 | サーバ・ダウンロード・表示（テーマ）の設定と、アカウント（ログアウト・別のサーバへ） | 表示専用の行を「ラベル上（overline）・値下（headline）」に揃える（#58）。#43 の詳細画面と同じ向き |
| 接続・ライブラリ選択 | 初回と切替のときだけ通る | 色以外は変えない |

### 共通の規則

- 値を見せる行は `overlineContent` = ラベル、`headlineContent` = 値。操作の行は `headlineContent` = 操作名、`supportingContent` = 説明。少数の選択肢から 1 つ選ぶ行は `overlineContent` = ラベル、`headlineContent` = `FilterChip` の並び（保持ルールの「最新 N 回」、設定の「テーマ」）
- 状態の色は `primary`（よく聴く・同期対象・再生中）と `error`（消失・失敗・破壊的操作）だけ。聴いている回の行の背景は `secondaryContainer`
- ボトムシートは「その画面に留まったまま 1 つ選ぶ」ときに使い、スクロールする長い一覧は画面にする（#43 の判断）
- ジャケット・波形など、データに無い視覚要素で埋めない（ラジオ録音にジャケットは無い。偽の波形も置かない）

## 検討の記録

5 つの案を、同じ 5 軸（① 行の情報密度、② 階層、③ 絞り込みの入口、④ 色、⑤ 再生画面の主役）の上に置いて比べた。画面イメージは HTML で描き（[claude.ai/artifact/DNLd8CXT26Jc5SjN86uGDP](https://claude.ai/artifact/DNLd8CXT26Jc5SjN86uGDP)、補助）、実機での `/prototype` は行っていない。

| 案 | 大事にすること | 捨てるもの |
| --- | --- | --- |
| **A 棚** | 今の 3 階層を磨く。絞り込みを 1 つの入口に | 個性（色を別に決めれば補える） |
| B 番組表 | 放送局を最上位に。局タブ・局ごとの色 | 「よく聴く」の節。局が 1 つの利用者には効かない。局名の表記ゆれで色が割れる |
| C 続きから | ボトムナビ「聴く｜番組」を足し、番組横断で続き → 未再生 | 3 階層の単純さ。下部が 2 段になる。#41 の行の一部を各回一覧へ戻す |
| **D 受信機** | 固定パレット（温かい黒＋アンバー）、等幅数字、大きな時間表示 | Material You との親和性 |
| E 一枚の紙 | 番組行をバッジだけに、再生画面を巨大なボタンだけに | #41・#45、一覧からの ★。毎晩使うスリープが 2 タップになる |

**選んだのは構造 A ＋ 色 D。** 主な聴取シーンが「片手で続きを流す」なので C か E に寄せる案もあったが、

- C はホームを足す分だけ下部（ミニプレイヤー＋ボトムナビ）が厚くなり、番組をまたいで流す機能が無い今は「聴く」タブの価値が半分になる
- E はスリープタイマーを隠すのが寝る前のシーンと逆行する

ので、構造は変えず（A）、片手の要件は再生画面の操作を下半分に寄せること（#57）で満たす。色は D で、D の再生画面（大きな時間表示・太いトラック）は採らない。ライトも用意する（ダーク主）。

## 進め方

1. #54 テーマ（先に入れる。他の見た目の最終確認に要る）
2. #55 番組一覧、#56 各回一覧、#57 再生画面、#58 設定 — 互いに独立、順不同
3. #59 ミニプレイヤー — #54 の後

実機で見て方針と食い違ったら、この文書を直してから実装を変える。
